services:
  db:
    image: postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
        POSTGRES_USER: postgres
        POSTGRES_PASSWORD: postgres
        POSTGRES_DB: prewave
    volumes:
      - ./src/main/resources/db:/docker-entrypoint-initdb.d